apiVersion: v1
kind: Service
metadata:
  name: {{ include "maintenant.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "maintenant.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "maintenant.selectorLabels" . | nindent 4 }}
